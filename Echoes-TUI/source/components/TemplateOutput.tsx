import React from 'react';
import {Text} from 'ink';
const TemplateOutput = ({selectedOption}: {selectedOption: string}) => {
	return <Text>{selectedOption}</Text>;
};

export default TemplateOutput;
